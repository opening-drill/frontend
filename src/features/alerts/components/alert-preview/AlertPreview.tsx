import { Typography, Box, Stack } from "@mui/material";

const AlertPreview = () => {
    <Box onClick={() => {}}>
        <Stack direction="row">
            <Typography>אירוע אירוע</Typography>
        </Stack>
    </Box>
};

export default AlertPreview;